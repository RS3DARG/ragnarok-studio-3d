Coloca aquí tus archivos estáticos públicos.

Sugeridos:
- og-image.jpg        -> imagen para compartir en redes (1200x630). Referenciada en src/app/layout.tsx (Open Graph / Twitter).
- favicon.ico         -> ícono del sitio.
- logo.svg / logo.png -> opcional; el logo principal es un wordmark en SVG (src/components/Logo.tsx) y no requiere archivo.

Las imágenes de figuras y categorías NO van aquí: se suben desde el panel
admin y se guardan en Supabase Storage (bucket "figures").
